import SwiftUI

struct AppColors {
    // Brand Colors
    static let maroon = Color(red: 0.55, green: 0.15, blue: 0.2)
    static let lightPink = Color(red: 0.98, green: 0.85, blue: 0.86)
    
    // General Colors
    static let background = Color(white: 0.98)
    static let surface = Color.white
    static let primary = Color.blue
    static let text = Color.black
    static let secondaryText = Color.gray
    static let accent = Color.orange
    static let coral = Color(red: 255/255, green: 107/255, blue: 107/255)
    static let PrimaryColor = Color(red: 0.55, green: 0.15, blue: 0.2)
}
